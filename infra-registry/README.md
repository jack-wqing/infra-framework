# infra-registry

## 配置项

```yaml
registry:
    nacos:
        appId: ${spring.application.name}
        clusterName: public
        host: localhost
        password: nacos
        port: 8848
        username: nacos
```
